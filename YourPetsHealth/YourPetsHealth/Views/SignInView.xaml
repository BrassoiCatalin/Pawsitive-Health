<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="YourPetsHealth.Views.SignInView"
             BackgroundImageSource="gradient.png">
    <StackLayout>
        <Label Text="Welcome to YourPetsHealth!"
               Style="{StaticResource SignInLabel}"></Label>
        <Entry Placeholder="Enter username..."/>
        <Entry Placeholder="Enter password..."
               IsPassword="True"/>

        <StackLayout Orientation="Horizontal"
                     HorizontalOptions="Center"
                     VerticalOptions="EndAndExpand"
                     Padding="0, 0, 0, 30">
            <Button Text="LogIn" Style="{StaticResource SignInButtonStyle}"/>
            <Button Text="SignUp" Style="{StaticResource SignInButtonStyle}"/>
            <Button Text="Forgot Password" Style="{StaticResource SignInButtonStyle}"/>
        </StackLayout>
    </StackLayout>
</ContentPage>